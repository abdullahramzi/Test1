const React = require('react');
const ReactDOM = require('react-dom');

function App() {
  return React.createElement('div', null, 'E-commerce Client');
}

ReactDOM.render(React.createElement(App), document.getElementById('root'));
